/**
 * Web panel registry — mirrors the desktop's webPanels.config.ts. The Files
 * panel is the same embedded app the desktop docks as a child webview; the
 * web client hosts it in an iframe. URL is a compile-time constant by design.
 */
export const FILES_PANEL_URL =
  "https://crichton.tailb3d4b8.ts.net:6201/?panel=files";
